import React from 'react';
import { View } from 'react-native';

export default function MoreTabPlaceholder() {
  // This screen will never render because tabPress is intercepted.
  return <View />;
}

