# PATIENT APPLICATION
│
├── SCREEN 1 — Dashboard (`/home`)
│   ├── Screen 1.1 — Notifications (`/notifications`)
│   ├── Screen 1.2 — AI Assistant (`/ai`)
│   ├── Screen 1.3 — Health Overview (`/health`)
│   │   ├── Screen 1.3.1 — Medical History (`/health/medical`)
│   │   ├── Screen 1.3.2 — Caregivers (`/health/caregivers`)
│   │   ├── Screen 1.3.3 — Care Plans (`/health/careplans`)
│   │   ├── Screen 1.3.4 — Clinical Outcomes (`/health/clinical-outcomes`)
│   │   ├── Screen 1.3.5 — Prescriptions (`/health/prescriptions`)
│   │   └── Screen 1.3.6 — Recurring Care (`/health/recurring`)
│   └── Screen 1.4 — Medical Records (`/records`)
│       ├── Screen 1.4.1 — Vitals (`/records/vitals`)
│       └── Screen 1.4.2 — Risk History (`/records/risk-history`)
│
├── SCREEN 2 — Care Requests (`/requests`)
│   ├── Screen 2.1 — Create Care Request (`/requests/new`)
│   └── Screen 2.2 — Request Details (`/requests/[id]`)
│
├── SCREEN 3 — Marketplace (`/marketplace`)
│   ├── Screen 3.1 — Offers List (`/marketplace/[id]`)
│   ├── Screen 3.2 — Contracts (`/marketplace/contracts`)
│   └── Screen 3.3 — Contract Details (`/marketplace/contracts/[id]`)
│
├── SCREEN 4 — Visits (`/visits`)
│   └── Screen 4.1 — Visit Details (`/visits/[id]`)
│
├── SCREEN 5 — Messages (`/messages`)
│   └── Screen 5.1 — Chat (`/messages/chat`)
│
└── SCREEN 6 — Profile (`/profile`)
    ├── Screen 6.1 — Edit Profile (`/profile/edit`)
    ├── Screen 6.2 — Emergency Contacts (`/profile/emergency-contacts`)
    └── Screen 6.3 — Settings (`/profile/settings`)
